let sumar = require("./sumar");
let restar = require("./restar");
let dividir = require("./dividir");
let multiplicar = require("./multiplicar");

/*console.log(sumar(2, 2));
console.log(restar(4, 2));
console.log(multiplicar(2, 2));
console.log(multiplicar(2, 0));
console.log(dividir(2, 2));
console.log(dividir(2, 0));
*/

//a.¿Qué hubiese sucedido si, en vez de generar un archivo por cada operaciónmatemática, hubiésemos programado todo en un mismo archivo?

/*const calculadora = {
    sumar: function (a, b) { return a + b },
    restar: function (a, b) { return a - b },
    dividir: function (a, b) { if (a == 0 || b == 0) { return "No se puede dividir por cero" } else { return a / b } },
    multiplicar: function (a, b) { return a * b }
}
*/